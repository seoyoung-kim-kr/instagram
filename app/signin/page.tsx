"use client";

import GradientBtn from "@/components/GradientBtn";

export default function page() {
  return (
    <section className="flex items-center py-10 justify-center">
      <GradientBtn text="Sign In with Google" onClick={() => {}} />
    </section>
  );
}
